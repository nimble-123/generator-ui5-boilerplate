{
  "name": "<%= projectName %>",
  "version": "0.0.1",
  "description": "<%= projectDescription %>",
  "private": true,
  "author": "<%= projectAuthor %> <<%= projectAuthorEmail %>>",
  "repository": {
    "type": "git",
    "url": "<%= gitRepository %>.git"
  },
  "bugs": {
    "url": "<%= gitRepository %>/issues"
  },
  "homepage": "<%= gitRepository %>.git#readme",
  "scripts": {
    "build": "grunt",
    "test": "grunt unit_and_integration_tests",
    "deploy": "grunt deploy"
  },
  "devDependencies": {
    "@sap/grunt-sapui5-bestpractice-build": "^1.3.58",
    "@sap/grunt-sapui5-bestpractice-test": "^0.0.1"
  },
  "dependencies": {
    "@openui5/sap.m": "^<%= minimumVersion %>",
    "@openui5/sap.ui.core": "^<%= minimumVersion %>",
    "@openui5/sap.ui.layout": "^<%= minimumVersion %>",
    "@openui5/themelib_sap_belize": "^<%= minimumVersion %>"
  }
}
